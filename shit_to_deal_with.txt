brew upgrade node
The following node executables are shadowed by other commands earlier in your PATH:
  node (shadowed by /Users/robertjoost/.config/nvm/versions/node/v22.18.0/bin/node)
    npm (shadowed by /Users/robertjoost/.config/nvm/versions/node/v22.18.0/bin/npm)
      npx (shadowed by /Users/robertjoost/.config/nvm/versions/node/v22.18.0/bin/npx)
      Running these by name will not invoke the version provided by Homebrew.
      Disable this behaviour by setting `HOMEBREW_NO_PATH_SHADOW_CHECK=1`.
      Hide these hints with `HOMEBREW_NO_ENV_HINTS=1` (see `man brew`).


4. pyenv — add to ~/.zprofile
  export PYENV_ROOT="$HOME/.pyenv"
  export PATH="$PYENV_ROOT/bin:$PATH"
  eval "$(pyenv init --path)"

  5. pyenv — add to ~/.zshrc
  command -v pyenv >/dev/null && eval "$(pyenv init -)"

  6. Open a new terminal, then:
  pyenv install 3.12.4
  pyenv global 3.12.4
  pip install pynvim debugpy ansible ansible-lint yamllint ruff

  7. pyright
  npm install -g pyright

  8. GitHub CLI auth
  gh auth login

  9. Install a Nerd Font
  Download and install Berkeley Mono Nerd Font (or any Nerd Font), set it in your terminal app.

  10. Clone the config
  git clone https://github.com/rjoost/astronvim_config.git ~/.config/nvim
  cd ~/.config/nvim
  git checkout sre-overhaul

  11. Launch nvim and wait
  nvim
  Lazy.nvim bootstraps, then installs ~60 plugins. Mason then auto-installs lua_ls, ansiblels, dockerls, stylua, ruff, yamllint, ansible-lint, python DAP adapter, and Treesitter parsers. Don't close nvim until everything
  finishes. Watch with :Lazy and :Mason.

  12. Verify
  :checkhealth          → no errors
  :Mason                → all green
  open a .py file → :LspInfo   → pyright attached
  open a .yml file → :LspInfo  → yamlls attached
  <Leader>gg            → lazygit opens
  s (normal mode)       → flash jump works

  ---
  That's it. Steps 4–5 assume you're setting up .zprofile/.zshrc fresh — if they already exist on the new machine, just append those lines.


# THEME?
for iterm
# USGC BASH PROMPT
if [[ $- == *i* ]]; then
  export CLICOLOR=1
  export LSCOLORS=GxFxCxDxBxegedabagaced
  export PS1="\[$(tput setaf 7)\]❬\h❭ \[$(tput setaf 2)\]\W\[$(tput setaf 1)\] ●\[$(tput sgr0)\] "
fi
